# common profile 
export VISUAL='vim'
export EDITOR="$VISUAL"

[ -f "$HOME/.sh_aliases" ] && source $HOME/.sh_aliases
for f in $HOME/.sh_helpers/*; do source $f; done

# fzf
export FZF_TMUX=1
export FZF_DEFAULT_OPTS="--height 40%"
[ -f "$HOME/.fzf.zsh" ] && source $HOME/.fzf.zsh
